'use client'

import { Typography } from '@mui/material'

const ContactPage: React.FC = () => {
  return (
    <div
      style={{
        marginTop: '200px',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Typography variant="h3"> ContactPage</Typography>
    </div>
  )
}

export default ContactPage
